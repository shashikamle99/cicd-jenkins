pipeline{
    agent{ dockerfile true }
    stages {
      stage('test') {
        steps {
          sh '''
            mvn --version
            git --version
            curl --version
          '''
            }
        } 
    }
}